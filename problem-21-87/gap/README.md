# GAP workspace

Reserved for finite generation and subgroup-index experiments for Problem
21.87.

A future predicate must expose:

- the chosen value of \(d\);
- generators for every subgroup in the gcd certificate;
- subgroup indices and their independently recomputed gcd;
- evidence for \(d(G)\), including a lower bound as well as a generating set;
- an explicit `UNKNOWN` state when subgroup enumeration or the generator
  lower bound is incomplete.
