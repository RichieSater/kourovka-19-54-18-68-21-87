# GAP workspace

Reserved for the Problem 19.54 property implementation and finite subgroup-
lattice certificates.

Before adding code, `ATTACK.md` Gate A requires:

- two independently stated predicates;
- explicit subgroup embedding/overgroup handling;
- named positive and negative fixtures;
- fail-closed behavior when subgroup-lattice data are incomplete.

Do not copy `MMCLast` from the monotone-chain project and rename it: that
recurrence decides a different property.
